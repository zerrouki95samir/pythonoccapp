name: myproject  # overridden at install, so this is for your use with conda env
channels:
  - conda-forge
dependencies:
  - anaconda
  - pip
  pip:
    - flask
    - gunicorn
    - requests
    - httpbin